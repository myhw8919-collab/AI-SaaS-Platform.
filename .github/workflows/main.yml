name: AI-Update-Robot
on:
  repository_dispatch:
    types: [new-idea]

jobs:
  update-site:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: AI Processing
        run: |
          echo "AI verwerkt nu je idee: ${{ github.event.client_payload.idea }}"
          # Hier komt de koppeling die je site echt verandert
